version: "3.4"
networks:
  proxylampy:
    external: true
  serverlampy:
    external: true
  net:
    driver: overlay
    attachable: true
services:
  www:
    image: koromerzhin/php:8.4.13-apache
    depends_on:
      - mariadb
      - mercure
      - mailhog
      - redis
    volumes:
      - ${PWD}/apps:/var/www/:rw
      - ${PWD}/conf/php.ini-development:/usr/local/etc/php/conf.d/php.ini
      - ${PWD}/conf/apache2/000-default.conf:/etc/apache2/sites-available/000-default.conf:ro
      - ${PWD}/conf/apache2/apache2.conf:/etc/apache2/apache2.conf:ro
      - ${PWD}/conf/init-laravel.sh:/usr/local/bin/init-laravel.sh:rw
    command: >
      sh -c "
        chmod +x /usr/local/bin/init-laravel.sh &&
        /usr/local/bin/init-laravel.sh &&
        apache2-foreground
      "
    networks:
      - net
      - serverlampy
      - proxylampy
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.laravel.rule=Host(`laravel.traefik.me`)"
        - "traefik.http.routers.laravel-tls.tls.domains[0].main=laravel.traefik.me"
        - "traefik.http.routers.laravel.tls=true"
        - "traefik.http.services.laravel.loadbalancer.server.port=80"